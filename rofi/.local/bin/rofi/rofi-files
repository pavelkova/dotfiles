#!/usr/bin/env fish

set rofi_path (find "$HOME" -type d -path '*/\.*' -prune -o -not -name '.*' -type d | rofi -dmenu -p "🗊")

if [ -n "$rofi_path" ]
    echo $rofi_path
    exec kitty -e fish -c "cd $rofi_path; fish -i"
end
